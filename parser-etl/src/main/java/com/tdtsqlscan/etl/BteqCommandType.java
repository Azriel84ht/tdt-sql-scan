package com.tdtsqlscan.etl;

public enum BteqCommandType {
    LOGON,
    SET,
    LOGOFF,
    ACTIVITYCOUNT,
    ERRORCODE,
    DATABASE,
    EXPORT,
    IMPORT,
    IF,
    ENDIF,
    GOTO,
    LABEL,
    RUN,
    DECLARE,
    EXIT,
    OTHER,
    SQL
}
