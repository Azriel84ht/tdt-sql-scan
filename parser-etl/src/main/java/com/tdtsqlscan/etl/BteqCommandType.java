package com.tdtsqlscan.etl;

public enum BteqCommandType {
    LOGON,
    LOGOFF,
    ACTIVITYCOUNT,
    ERRORCODE,
    DATABASE,
    EXPORT,
    IMPORT,
    IF,
    ENDIF,
    GOTO,
    LABEL,
    RUN,
    DECLARE,
    OTHER,
    SQL
}
